# 上下文与进度管理
- 项目架构设计文档: `implementation_plan.md.resolved`
- 调试页面: `http://<host>:8080/debug.html`

# 基础设定
- 总是用中文回答，并为所有代码添加清晰的中文注释。

# 后端技术栈约束 (Java)
- 必须使用 JDK 8 (Java 1.8) 的语法。
- 框架强制使用 Spring Boot 2.7.18。
- ORM 框架使用 MyBatis-Plus 3.5.3，禁止手写 JDBC。
- 允许并鼓励使用 Lombok（@Data, @Slf4j 等）。
- 开发阶段使用 H2 内存数据库，生产切 MySQL 8.0。

# 核心架构（已取代旧 ES 架构）
- **数据流**: C# Agent → WebSocket → Java Server（实时推送，非定时查询）
- **WS 协议**: 统一信封 `{ type, agentId, timestamp, payload }`
- **规则引擎**: DB 规则 → rebuildMatchers() → 内存 AC 自动机，日志过滤不查 DB
- **过滤链**: CriticalRuleFilter(10) → ExcludeRuleFilter(20) → BasicFeatureFilter(30)
- **健康模型**: GREEN / YELLOW / RED 三色评估
- **架构红线**: 禁止引入 Elasticsearch、Logstash、Filebeat

# 包结构
```
com.example.demo
├── common/enums/       # MessageType, CommandAction, LogLevel, HealthStatus
├── common/result/      # Result<T> 统一响应
├── config/             # WebSocketConfig, EngineConfig, CorsConfig
├── engine/filter/      # LogFilter 责任链, LogFilterChain
├── engine/matcher/     # AhoCorasickMatcher, RegexMatcherCache
├── engine/model/       # LogContext, FilterResult
├── module/rule/        # FilterRule CRUD + 热加载 (REST /api/rules)
├── monitor/health/     # HealthEvaluator, ServerHealthState
├── monitor/alert/      # AlertService
└── websocket/          # AgentWebSocketHandler, MessageDispatcher, AgentSessionManager
```

# 代码规范
- Controller 必须返回 `Result<T>`（code, message, data），禁止返回裸对象
- 全局异常拦截: `@RestControllerAdvice` + `@ExceptionHandler`
- 增删改规则后自动 rebuildMatchers()，无需手动触发